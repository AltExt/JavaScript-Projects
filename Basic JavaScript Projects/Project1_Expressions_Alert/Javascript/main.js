function ButonPress() {
	var s1 = "This is the beginning of the string ";
	var s2 = "and this is the end of the string";
	alert(s1); // sends alert with s1
	alert(s2); // sends alert with s2
	var fullString = s1 + s2; // concatonate strings
	document.write(fullString); // print to document
}